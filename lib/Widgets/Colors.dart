import 'package:flutter/material.dart';

const backgroundcolor = Color.fromARGB(255, 200, 237, 240);
const textcolor = Color.fromARGB(255, 168, 173, 177); // Dark Blue
const appbarcolor = Color.fromARGB(255, 3, 27, 43); // Light Blue
const messagecolor = Colors.black;
const sendermessagecolor = Color.fromARGB(255, 94, 135, 166); // Dark Blue
const searchBarColor = Color.fromRGBO(99, 134, 148, 1); // Grayish Blue
const dividerColor = Color.fromRGBO(14, 122, 190, 1); // Blue
const chatBarMessage = Color.fromRGBO(12, 135, 218, 1); // Bright Blue
const mobileChatBoxColor = Color.fromRGBO(162, 195, 216, 1); // Light Blue

// Additional colors for a good combination
const accentColor = Colors.orange; // You can change this to another color that complements blue
const secondaryColor = Colors.grey; // You can change this to another color that complements blue
